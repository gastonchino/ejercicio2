function sumar(a, b) {
    return a + b;
  }
  
  console.log(sumar(3, 5)); // Salida: 8
  

  const sumarArrow = (a, b) => {
    return a + b;
  };
  
  console.log(sumarArrow(3, 5)); // Salida: 8
  
  (() => {
    
  })();

  () => {
    // Cuerpo de la función
  }; 

  const algo = function(){

  }