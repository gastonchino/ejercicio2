/*
Escribir un programa que declare un numero entero con cualquier valor e indique 
si dicho número es par o impar.
*/
// Par o Impar

// Declarar un número entero con cualquier valor
const numero = 17; // Puedes cambiar este valor según tus necesidades

// Verificar si el número es par o impar
if (numero % 2 === 0) {
    console.log(`${numero} es un número par.`);
} else {
    console.log(`${numero} es un número impar.`);
}
