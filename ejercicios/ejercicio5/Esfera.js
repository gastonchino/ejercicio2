// Esfera
const radioEsfera = 4;

// Volumen de la esfera
const volumenEsfera = (4/3) * Math.PI * Math.pow(radioEsfera, 3);

// Área de la esfera
const areaEsfera = 4 * Math.PI * Math.pow(radioEsfera, 2);

console.log(`c) Esfera: Volumen = ${volumenEsfera}, Área = ${areaEsfera}`);
